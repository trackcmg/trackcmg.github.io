const { chromium } = require('playwright'); const { spawn } = require('child_process'); const server = spawn('python', ['-m', 'http.server', '8082']); setTimeout(async () => { const browser = await chromium.launch(); const page = await browser.newPage(); page.on('pageerror', err => console.log('PAGE ERROR STACK:', err.stack || err.message || err)); await page.goto('http://localhost:8082/index.html', { waitUntil: 'networkidle' }); await browser.close(); server.kill(); process.exit(0); }, 2000);
